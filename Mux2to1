module mux2to1(
  input logic [1:0] a,   // 2-bit input signal a[1], a[0]
  input logic sel,       // Select signal
  output logic y);       // 1-bit output
  
  assign y = sel ? a[1]:a[0]; //sel = 1 selects a[1], and sel = 0 selects a[0]
endmodule
